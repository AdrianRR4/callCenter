import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sol4i.callCenter',
  appName: 'callCenter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
